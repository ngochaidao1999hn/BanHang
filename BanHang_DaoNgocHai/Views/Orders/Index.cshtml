@model IEnumerable<BanHang_DaoNgocHai.Models.Orders>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Clients.ClientName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OrderDetails.OrderId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Clients.ClientName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OrderDetails.OrderId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OrdId }) |
            @Html.ActionLink("Details", "Details", new { id=item.OrdId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OrdId })
        </td>
    </tr>
}

</table>
